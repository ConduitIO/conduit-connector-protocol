version: v1
name: buf.build/conduitio/conduit-connector-protocol
deps:
  - buf.build/conduitio/conduit-commons
lint:
  service_suffix: Plugin
  ignore_only:
    SERVICE_SUFFIX:
      - conduit/v1/schema.proto
  use:
    - DEFAULT
  except:
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
